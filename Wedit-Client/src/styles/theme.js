const fontSize = {
    small: "1.4rem",
    medium: "1.6rem",
    large: "2rem",
    xlarge: "2.4rem",
};

const font = {
    regular: {
        fontWeight: 400,
    },
    medium: {
        fontWeight: 500,
    },
    semibold: {
        fontWeight: 600,
    },
    bold: {
        fontWeight: 700,
    },
};

const theme = {
    fontSize,
    font,
};

export default theme;
