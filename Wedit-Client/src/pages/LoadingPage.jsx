import styled from 'styled-components';

const LoadingPage = () => {
	return <LoadingText>로딩 페이지</LoadingText>;
};

export default LoadingPage;

// CSS
const LoadingText = styled.p`
	color: black;
`;
