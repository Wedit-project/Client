import styled from 'styled-components';

const PhotoSelectionPage = () => {
	return <PhotoSelectionText>사진 선택 페이지</PhotoSelectionText>;
};

export default PhotoSelectionPage;

// CSS
const PhotoSelectionText = styled.p`
	color: black;
`;
